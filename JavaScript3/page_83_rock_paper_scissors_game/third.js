var Array = ["rock", "paper", "scissors"];

var randomResult;
var comindex = Math.random * 2;
var myindex = Math.random * 2;

function Checker() {
  if (Array[comindex] == Array[myindex]) {
    randomResult = console.log("tie");
  } else if (Array[comindex] == Array[myindex]) {
  }
}
